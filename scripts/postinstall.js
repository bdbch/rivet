// postinstall: placeholder for future platform-specific binary downloads.
// Currently the CLI is built from source (requires Rust toolchain).
// Run `yarn build:cli` or `npm run build:cli` to build the oxrls binary.
console.error('oxrls CLI binary not included in npm package.');
console.error('To build it, run: yarn build:cli');
console.error('Or install via cargo: cargo install --path .');
